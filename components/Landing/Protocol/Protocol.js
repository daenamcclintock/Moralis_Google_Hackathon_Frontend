import React from 'react'
import { ProtocolContainer } from './ProtocolStyles'

const Protocol = () => {
  return (
    <ProtocolContainer>
        Protocol
    </ProtocolContainer>
  )
}

export default Protocol